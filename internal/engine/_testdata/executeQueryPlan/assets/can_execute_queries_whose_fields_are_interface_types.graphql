# can execute queries whose fields are interface types
query {
    user(id: "1") {
        name {
            first
            last
        }
        vehicle {
            description
            price
            retailPrice
        }
    }
}
