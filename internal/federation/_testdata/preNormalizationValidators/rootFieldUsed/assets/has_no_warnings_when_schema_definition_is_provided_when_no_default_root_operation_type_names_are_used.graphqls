# option:name: schemaDefinition
# rootFieldUsed
# has no warnings when a schema definition / extension is provided, when no default root operation type names are used
schema {
    query: RootQuery
}

type RootQuery {
    product: Product
}

type Product {
    sku: String
}
