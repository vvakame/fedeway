# option:name: Accounts
# option:url:  http://accounts.example.com/query
extend type Query {
    me: User
}
type User @key(fields: "id") {
    id: ID!
    name: String
    username: String
    birthDate: String
}
