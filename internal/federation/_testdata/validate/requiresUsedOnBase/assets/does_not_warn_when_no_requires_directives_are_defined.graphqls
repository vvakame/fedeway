# option:name: serviceA
# requiresUsedOnBase
# does not warn when no requires directives are defined
type Product @key(fields: "color { id value }") {
    sku: String!
    upc: String!
    color: Color!
}

type Color {
    id: ID!
    value: String!
}
