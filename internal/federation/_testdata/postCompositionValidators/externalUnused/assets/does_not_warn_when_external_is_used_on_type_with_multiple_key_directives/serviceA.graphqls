# option:name: serviceA
# externalUnused
# does not warn when @external is used on type with multiple @key directives
type Product @key(fields: "upc") @key(fields: "sku") {
    upc: String
    sku: String
}
