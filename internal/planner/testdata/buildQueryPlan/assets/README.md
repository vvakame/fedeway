# How to create supergraphSdl.graphqls

```shell
$ git clone git@github.com:apollographql/federation.git
$ cd federation
$ npm ci
$ patch -p1 < ../patch.diff
$ npm test
$ mv supergraphSdl.graphqls ../
```
